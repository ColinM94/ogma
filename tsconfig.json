{
    "extends": "expo/tsconfig.base",
    "compilerOptions": {
        "strict": true,
        "baseUrl": ".",
        "paths": {
            "api*": ["src/api*"],
            "components*": ["src/components*"],
            "common*": ["src/common*"],
            "contexts*": ["src/contexts*"],
            "library*": ["src/library*"],
            "navigation*": ["src/navigation*"],
            "screens*": ["src/screens*"],
            "assets*": ["src/assets*"]
        }
    }
}
